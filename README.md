# experiment-electron-rust-ui

An experimental Electron UI with a Rust backend

## Prerequisites
- Node
- Rust

## Build and run
```bash
$ cd core
$ cargo build
$ cd ../ui
$ nvm use
$ npm install
$ npm start
```